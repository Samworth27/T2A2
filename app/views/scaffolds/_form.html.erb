<%= form_with(model: scaffold) do |form| %>
  <% if scaffold.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(scaffold.errors.count, "error") %> prohibited this scaffold from being saved:</h2>
      <ul>
        <% scaffold.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.submit %>
  </div>
<% end %>
